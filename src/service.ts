import { } from './interfaces';

// example service. Put all your actual functionality in services!
// each service implements a class

class ServiceManager {

    constructor() { }

}

export default new ServiceManager();
